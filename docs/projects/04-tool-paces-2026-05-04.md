# Moment Studio tool paces — 2026-05-04

Branch: `plan/moment-studio-v1`
Local path: `/Users/nicanadian-macmini/moment-video-maker`
Run time: 2026-05-04 17:16 PDT

## Summary

I exercised the CLI through a synthetic but real local workflow using a throwaway `SOLYPSIZM_HOME`, generated media fixtures with `ffmpeg`, and ran safe API/automation/benchmark smoke paths without making paid provider calls.

## Manual end-to-end workflow

Result: PASS.

Throwaway run log:
`/tmp/soly_e2e-20260504171029.log`

Workflow covered:

1. `solypsizm bootstrap-brand-kit --source ./brand-kit.json`
2. `solypsizm new haze --song-title ... --lyrics ... --audio ...`
3. `solypsizm import-concepts ...`
4. `solypsizm pick-concept concept-01-signal-tower-dawn`
5. `solypsizm import-scenes ...`
6. `solypsizm prompts scene-01-tower-wakeup --no-copy`
7. Imported start/end frames for 3 scenes.
8. Imported 3 synthetic MP4 takes.
9. `solypsizm analyze`
10. `solypsizm suggest-moments --count 3 --strategy section_focus`
11. `solypsizm list-scenes`
12. `solypsizm list-moments`
13. `solypsizm review-moment moment-01 --approve`
14. `solypsizm trace take-01.mp4`
15. `solypsizm render-moment moment-01`
16. `solypsizm status`
17. `solypsizm doctor`
18. `solypsizm log --tail 20`

Observed output highlights:

- Project created successfully.
- 1 concept imported and picked.
- 3 scenes imported.
- 3 frame pairs imported.
- 3 clips imported.
- Audio analysis completed: 4 sections, BPM 117.45, duration 36.0s.
- Moment suggestion produced 2 specs from the synthetic short-clip setup.
- `moment-01-intro` approved.
- `trace take-01.mp4` found two moment usages.
- `render-moment` produced the current v1 Variant Builder handoff message.
- `doctor` reported no issues.

Note: the synthetic audio/clip lengths needed a permissive temporary `edit-config.json`; with defaults, the 36s generated test song sections were too short to host a default-length moment. This is expected for the tiny smoke fixture, not necessarily a product bug.

## API / automation / benchmark safe smokes

### `prompts --api`

Found and fixed two issues:

1. `_run_prompts_api` referenced `start_frame_prompt`, `end_frame_prompt`, and `veo_motion_prompt` without importing them.
2. Invalid provider specs raised a raw traceback (`ValueError`) instead of a Click-friendly error.

Verification:

- Added regression tests in `tests/test_prompts_api.py`.
- Direct invalid-provider smoke now reports:
  `Error: Unknown image provider 'fake'.`
  instead of a stack trace.

### `auto`

Safe existing-project smoke on the completed throwaway project:

- `solypsizm auto --moments 2 --review-gates none --budget 0.01 --text-model fake:no-call --image-model fake:no-call --video-model fake:no-call`
- Because concepts/scenes/clips/analysis/moments already existed, it skipped provider stages and completed cleanly.
- `solypsizm auto-resume` correctly reported the pipeline was already complete.

Logs:

- `/tmp/soly_auto_existing_e2e.txt`
- `/tmp/soly_auto_resume_existing_e2e.txt`

### `benchmark`

Found and fixed one issue:

- Budget-refusal errors bubbled as a raw `ProviderError` traceback.
- `run_benchmark` now wraps `ProviderError` as `click.ClickException`.

Verification:

- Added regression test in `tests/test_benchmark.py`.
- Safe budget-refusal smoke now reports:
  `Error: Estimated cost $0.01 exceeds --budget $0.00. Reduce --takes or trim the model list.`
  instead of a stack trace.

Log:

- `/tmp/soly_benchmark_budget_refusal_fixed.txt`

## Code changes

Functional fixes:

- `src/solypsizm_moment_studio/commands/scenes.py`
  - Import prompt template helpers inside `_run_prompts_api`.
  - Convert invalid provider resolution `ValueError`s into Click errors.
- `src/solypsizm_moment_studio/commands/benchmark.py`
  - Convert benchmark `ProviderError`s into Click errors.

Regression tests:

- `tests/test_prompts_api.py`
  - Covers successful mocked API prompt generation/import handoff.
  - Covers invalid provider Click-error wrapping.
- `tests/test_benchmark.py`
  - Covers benchmark provider error Click-error wrapping.

Cleanup:

- Ran `ruff check . --fix`; it applied safe autofixes across imports, f-string prefixes, and similar style-only issues.

## Verification commands

Passing:

```bash
. .venv/bin/activate
pytest -q
# 97 passed in 0.16s

git diff --check
python -m py_compile src/solypsizm_moment_studio/commands/scenes.py src/solypsizm_moment_studio/commands/benchmark.py
```

Current lint state:

```bash
ruff check . --statistics
# 38 E501 line-too-long
# 5 SIM105 suppressible-exception
# 2 SIM102 collapsible-if
# 1 B905 zip-without-explicit-strict
# Found 46 errors.
```

Lint improved from the previous broad baseline after safe autofix, but the repo is not fully lint-clean yet. Remaining issues are mostly long lines and small simplifications; no undefined-name blocker remains.

## Follow-up completed

The originally recommended cleanup items are now implemented:

1. Ruff is clean: `ruff check .` reports `All checks passed!`.
2. First-class dry-run modes were added:
   - `solypsizm prompts --api --dry-run`
   - `solypsizm benchmark --dry-run`
   - `solypsizm auto --dry-run`
3. A repeatable no-provider smoke harness was added:
   - `src/solypsizm_moment_studio/smoke.py`
   - `tests/test_cli_smoke_workflow.py`

Latest verification:

```bash
. .venv/bin/activate
ruff format .
ruff check .
pytest -q
# 101 passed in 0.31s
```

Remaining recommendation:

1. Eventually wire `render-moment` to real MP4 assembly; v1 still stops at Variant Builder handoff.
